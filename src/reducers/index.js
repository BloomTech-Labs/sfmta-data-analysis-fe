
const intitialState = {
    isLoading: false,
    error: ''
}

export const rootReducer = (state = intitialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}